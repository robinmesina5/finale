<%= form_for(@tree_role) do |f| %>
  <% if @tree_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tree_role.errors.count, "error") %> prohibited this tree_role from being saved:</h2>

      <ul>
      <% @tree_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :family_id %><br>
    <%= f.number_field :family_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
