<%= form_for(@family_tree) do |f| %>
  <% if @family_tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@family_tree.errors.count, "error") %> prohibited this family_tree from being saved:</h2>

      <ul>
      <% @family_tree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :relative_id %><br>
    <%= f.number_field :relative_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
